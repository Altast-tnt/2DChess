# 2D Chess Game (Oxygine Framework)

Тестовое задание на позицию C++ разработчика. Проект представляет собой 2D шахматы с реализацией базовой игровой логики, анимаций и системы захвата фигур.

## 📺 Демонстрация работы
<video src="https://github.com/user-attachments/assets/74e6663e-ad70-4a5e-bd09-8ad69d700f2c" width="100%" controls muted autoplay loop></video>

## 🎮 Функционал
- **Управление в два клика**: первый клик выбирает фигуру (подсвечивается желтым), второй — указывает целевую клетку или вражескую фигуру для атаки.
- **Плавные анимации**: перемещение фигур реализовано через систему `Tween` (плавный переход между координатами).
- **Механика взятия**: при захвате фигуры противника проигрывается анимация плавного исчезновения (`TweenAlpha`), после чего объект корректно удаляется из сцены и памяти.
- **Логика перемещений**: реализованы правила хода для всех типов фигур (пешки, ладьи, кони, слоны, ферзь, король).
- **Очередность хода**: реализована строгая логика смены хода между игроками. Попытка хода фигурой противника блокируется.

*Примечание: В рамках "минимального функционала" правила шаха, мата и рокировки не реализованы.*

## 🛠 Технологии
- **Язык**: C++11
- **Движок**: [Oxygine Framework](https://oxygine.org/)
- **Графический бэкенд**: SDL2 / OpenGL
- **Система сборки**: CMake

## 🚀 Сборка и запуск (Linux)

### 1. Установка зависимостей
Для Ubuntu:
```bash
sudo apt-get install cmake libsdl2-dev libjpeg-dev libpng-dev zlib1g-dev libgles2-mesa-dev
```

### 2. Компиляция
```bash
mkdir build
cd build
cmake ..
make -j4
```

### 3. Запуск
```bash
./ChessGame
```

## 📂 Структура проекта
Проект придерживается модульной структуры для разделения ответственности:
- `src/main.cpp` — точка входа, инициализация систем движка и запуск основного цикла.
- `src/ChessController.cpp/h` — контроллер, управляющий всей игровой логикой, обработкой событий и состоянием доски.
- `src/Piece.h` — сущность шахматной фигуры, управляющая своим типом, цветом и визуальным состоянием.
- `src/BoardCell.h` — класс логической клетки, обеспечивающий точное попадание кликов по полям доски.

## 📝 Технические решения
- **Иерархическая сетка**: Для исключения "сдвига" координат при масштабировании доски реализована сетка из невидимых объектов `BoardCell`, являющихся дочерними элементами спрайта доски. Это гарантирует сохранение логических координат при любых трансформациях родительского объекта.
- **Визуальная чистота**: Устранена проблема графических артефактов (зеленых рамок) вокруг фигур на темных полях путем настройки режима смешивания `blend_alpha` и корректной обработки альфа-канала текстур.
- **Управление ресурсами**: Реализована автоматическая очистка памяти через умные указатели `intrusive_ptr` и явное освобождение контейнеров ресурсов при выходе из приложения.
